spring:
  application:
    name: consul-user
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        register: true
        enabled: true
        instance-id: consul-user-8710
        health-check-url: http://192.168.96.52:8710

server:
  port: 8710
